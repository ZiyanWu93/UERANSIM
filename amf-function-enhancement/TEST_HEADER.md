# Integrate Header file into nflambda

# Make the direct function version
## let it include the header
### create the header file (copy)
### make it print all the fields of input message, in a user friendly way so that I can easily check whether the parsing is correct (matching of the field values between input message and messgage interpreation through the data structure)
### do the same for the output message